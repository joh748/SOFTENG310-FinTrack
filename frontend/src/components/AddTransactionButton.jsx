const handleAddTransactionButton = () => {

    const handleAddTransaction = async () => {
        // TODO: Add transaction
    }

    return (
        <button className='bg-primary-purple text-white text-button px-3 py-3 min-w-[280px] rounded-full hover:bg-primary-purple-dark active:bg-primary-pruple-darker' onClick={handleAddTransaction}>Add Transaction</button>
    );
}

export default handleAddTransactionButton;