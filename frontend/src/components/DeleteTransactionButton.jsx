const handleDeleteTransactionButton = () => {

    const handleDeleteTransaction = async () => {
        // TODO: Add transaction
    }

    return (
        <button className='bg-primary-red text-white text-button px-3 py-3 min-w-[280px] rounded-full hover:bg-primary-red-dark active:bg-primary-red-darker' onClick={handleDeleteTransaction}>Delete Transaction</button>
    );
}

export default handleDeleteTransactionButton;